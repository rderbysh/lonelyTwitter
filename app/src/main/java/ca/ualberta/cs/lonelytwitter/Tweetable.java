package ca.ualberta.cs.lonelytwitter;


import java.util.Date;

// Made the interface package-private
interface Tweetable {

    // Removed the unnecessary public in front of the interface modifier
    String getMessage();

    // Removed the unnecessary public in front of the interface modifier
    Date getDate();

}
